name: TimesNetFeatureExtractor
height: 64


is_fc: True # if false must be same dim_model

# timesnet(default)
dim_model: 512
encoder_layers: 2
times_blocks: 2
num_kernels: 6
dropout: 0.1
dim_fc: 2048
task: "classification" #"anomaly" #forecast

# 時間情報をエンコーディングするときに使用
# NOTE encoderとして使用するなら使わない
embed_encoding: "timeF"
freq: "s"

